2026-05-20

今日完成：
1. 基于 Scope Engine 输出的 todo_list，新增 System-Folder Mapper 逻辑。
2. 使用 2 个检查节点较完整的测试文件进行验证。
3. 支持根据 TargetSystem 默认匹配 supporting 文件夹。
4. 支持通过 system_folder_mapping.xlsx 处理 System 名称与实际文件夹名称不一致的情况。
5. 输出 todo_list_with_folder.xlsx，补充 ResolvedFolderName、ResolvedFolderPath、FolderMatchStatus、FolderMatchNote 等字段。
6. 输出 unmatched_systems.xlsx，用于后续人工维护未匹配系统。

当前状态：
System-Folder Mapper 初步跑通，后续可进入 Evidence Loader / OCR Extractor 阶段。